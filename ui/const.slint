export global GState {
    in-out property <bool> is_db_connected: false;
    in-out property <string> db_type;
    in-out property <bool> dialog_visible: false;
    in-out property <string> query_result;
    in-out property <bool> is_tested_connection: false;
}
